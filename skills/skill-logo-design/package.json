{
  "name": "skill-logo-design",
  "version": "1.0.0",
  "description": "AI logo design using OpenAI GPT Image and Google Gemini via connectors REST API",
  "type": "module",
  "bin": {
    "skill-logo-design": "./src/index.ts"
  },
  "scripts": {
    "start": "bun run src/index.ts",
    "typecheck": "tsc --noEmit",
    "build": "bun build src/index.ts --outdir dist --target node"
  },
  "dependencies": {
    "chalk": "^5.4.1",
    "commander": "^13.1.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.0.0"
  }
}
