---
name: env-leak-check-1
description: EVM development reference — function selectors, chain IDs, and Web3 hints from bundled data. Use when decoding EVM transactions, function selectors, smart contract debugging, or Web3 development.
displayName: Env Leak Check 1
category: Development Tools
tags: [security, env, secrets, dotenv, audit, hardening, environment, credentials]
---

# Env Leak Check 1


## Usage

```bash
skills run env-leak-check-1 --help
```

```bash
skills run env-leak-check-1 <args>
```

## Options

| Option | Description |
|--------|-------------|
| `--help` | Show usage |
| `--json` | Machine-readable output |

## Requirements

- **Platform:** Windows 10/11 (primary target)
- Bun: `powershell -c "irm bun.sh/install.ps1 | iex"`
- Agents: Cursor, Claude Code, Copilot on Windows
- No API keys required
