{
  "name": "@hasnaxyz/skill-analyze-data",
  "version": "0.1.0",
  "description": "Data science insights for CSV and JSON",
  "type": "module",
  "bin": {
    "skill-analyze-data": "./bin/cli.ts"
  },
  "scripts": {
    "dev": "bun run src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target node",
    "start": "bun run bin/cli.ts"
  },
  "dependencies": {
    "commander": "^13.1.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.7.0"
  },
  "publishConfig": {
    "registry": "https://registry.npmjs.org",
    "access": "restricted"
  }
}
