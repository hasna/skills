# secrets-scanner-2

Scan project files for leaked API keys, tokens, passwords, and private keys

## Usage

```bash
skills run secrets-scanner-2 --help
skills run secrets-scanner-2 --json
```

## Development

```bash
bun run src/index.ts --help
bun run typecheck
```
