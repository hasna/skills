# pre-deploy-checklist

Pre-deployment checklist for tests, security, env vars, migrations, and rollback readiness. Windows / PowerShell / cmd

## When to use

- ready to deploy
- production checklist
- go live review
- release readiness
- production release checklist

Search terms: deploy, checklist, release, production, security, audit, hardening, developer-tools

## Usage

```bash
skills run pre-deploy-checklist --help
skills run pre-deploy-checklist --json
```

## Development

```bash
bun run src/index.ts --help
bun run typecheck
```
