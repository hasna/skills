# onchain-skill-loader

On activation, read `data/reference.json` and keep in session context.

Use `common_selectors`, `chains`, and `hints` for the user's Web3 task.

Offline only. No network calls from this skill.
