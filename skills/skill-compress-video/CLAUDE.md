# skill-compress-video

Video compression skill that reduces file size while preserving visual quality. Uses ffmpeg-based compression with configurable quality presets.

## Development

- Runtime: Bun
- Use `bun run dev` to run locally
- Use `bun run typecheck` to check types
