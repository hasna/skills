# dockerfile-security-scan

Scan Dockerfiles for root user, secrets in layers, and container hardening issues. Windows / PowerShell / cmd. Tags: docker, dockerfile, security

## When to use

- dockerfile security
- run as root docker
- container hardening
- docker best practices
- dockerfile best practices

Search terms: docker, dockerfile, security, container, devops, audit, hardening, developer-tools

## Usage

```bash
skills run dockerfile-security-scan --help
skills run dockerfile-security-scan --json
```

## Development

```bash
bun run src/index.ts --help
bun run typecheck
```
