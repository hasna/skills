# secrets-scanner

Scan project files for leaked API keys, tokens, passwords, and private keys

## Usage

```bash
skills run secrets-scanner --help
skills run secrets-scanner --json
```

## Development

```bash
bun run src/index.ts --help
bun run typecheck
```
