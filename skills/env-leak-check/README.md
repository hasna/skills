# env-leak-check

Find committed. Windows / PowerShell / cmd

## When to use

- .env committed to git
- dotenv leak
- secrets in repo
- environment variable audit
- committed .env file

Search terms: security, env, secrets, dotenv, audit, hardening, environment, credentials

## Usage

```bash
skills run env-leak-check --help
skills run env-leak-check --json
```

## Development

```bash
bun run src/index.ts --help
bun run typecheck
```
